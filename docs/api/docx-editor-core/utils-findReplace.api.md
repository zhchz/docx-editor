## API Report File for "@eigenpal/docx-editor-core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export function createDefaultFindOptions(): FindOptions;

// @public
export function createSearchPattern(searchText: string, options: FindOptions): RegExp | null;

// @public
export function escapeRegexString(str: string): string;

// @public
export function findAllMatches(content: string, searchText: string, options: FindOptions): Array<{
    start: number;
    end: number;
}>;

// @public
export function findInDocument(document: any, searchText: string, options: FindOptions): FindMatch[];

// @public
export function findInParagraph(paragraph: any, searchText: string, options: FindOptions, paragraphIndex: number): FindMatch[];

// @public
export interface FindMatch {
    contentIndex: number;
    endOffset: number;
    paragraphIndex: number;
    startOffset: number;
    text: string;
}

// @public
export interface FindOptions {
    matchCase: boolean;
    matchWholeWord: boolean;
    useRegex?: boolean;
}

// @public
export interface FindResult {
    currentIndex: number;
    matches: FindMatch[];
    totalCount: number;
}

// @public
export function getDefaultHighlightOptions(): HighlightOptions;

// @public
export function getMatchCountText(result: FindResult | null): string;

// @public
export interface HighlightOptions {
    currentMatchColor: string;
    otherMatchColor: string;
}

// @public
export function isEmptySearch(searchText: string): boolean;

// @public
export function replaceAllInContent(content: string, searchText: string, replaceText: string, options: FindOptions): string;

// @public
export function replaceFirstInContent(content: string, searchText: string, replaceText: string, options: FindOptions, startIndex?: number): {
    content: string;
    replaced: boolean;
    matchStart: number;
    matchEnd: number;
};

// @public
export function scrollToMatch(containerElement: HTMLElement | null, match: FindMatch): void;

```
